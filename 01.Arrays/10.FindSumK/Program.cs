using System;
using System.Text;
/*
Write a program that finds in given array of integers a sequence of given sum S (if present). Example:	 {4, 3, 1, 4, 2, 5, 8}, S=11  {4, 2, 5}
*/
class Program
{
    static int sequence(int[] array, int k)
    {
        int MaxSoFar = array[0], MaxEnding = array[0], begin, begin_temp, end;
        begin = begin_temp = end = 0;

        for (int i = 1; i < array.Length; i++)
        {

   ;
    }
}

